services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./prisma/dev.db
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - db-data:/app/prisma
    restart: unless-stopped

volumes:
  db-data:
